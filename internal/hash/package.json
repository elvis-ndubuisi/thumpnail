{
  "name": "@thumpnailss/hash",
  "version": "0.0.1",
  "description": "Provides a convenient way to calculate SHA-256 hashes of strings or binary data and return them in a base64 encoded format, which is useful for various cryptographic applications",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tooling/typescript-config": "workspace:^",
    "typescript": "^5.3.3",
    "vitest": "^1.5.0"
  }
}
