import {Request, Response} from "express";
import sharp from "sharp";

export async function genHashController(req: Request, res: Response) {
  try {
  } catch (error) {
    return res.sendStatus(500);
  }
}
