{
  "name": "hash-service",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "clean": "rimraf node_modules .wrangler"
  },
  "dependencies": {
    "hono": "^4.2.7",
    "blur-forge": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240403.0",
    "wrangler": "^3.47.0"
  }
}
